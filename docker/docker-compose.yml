services:
  server:
    container_name: web-clipper-server
    image: web-clipper-server
    build:
      context: ..
    env_file:
      - ../.env
    ports:
      - "65330:65330"
    volumes:
      - ../uploads:/app/uploads
    restart: unless-stopped
